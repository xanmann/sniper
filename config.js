module.exports = {
    token: "", // place your token here (no token = QR Login)
    password: "", // The password is mandatory to change the nickname & tag
    username: "xvids", // The username to snipe (no username === no work)
    tag: "0001" // The tag to snipe (no tag === no work)
}